# Xcode
GLTFInspector.xcodeproj/
xcuserdata/
*.xcworkspace
DerivedData/
build/
.build/

# Local research / agent scratch (not the product)
.worktrees/
# Claude Code: keep project settings, drop the runtime lock
.claude/scheduled_tasks.lock
.cursor/*
!.cursor/skills/
!.cursor/skills/**
!.cursor/agents/
!.cursor/agents/**
!.cursor/hooks/
!.cursor/hooks/**
!.cursor/hooks.json
# Project MCP lives at /.mcp.json (shared by Claude + Cursor via symlink)
.xcodebuildmcp/
.conductor/
.dex/
.superpowers/
docs/
plans/
test-models/
Fixtures/
TestModels/Khronos samples/
.readme-preview.html
scripts/.cache/

# Sparkle release artifacts and local CLI cache
dist/
tools/sparkle/
sparkle_private_key.txt

# Generated plists (xcodegen recreates these)
PreviewExtension/Info.plist
ThumbnailExtension/Info.plist
GLTFInspector/Info.plist

# Entitlements (xcodegen recreates these)
PreviewExtension/PreviewExtension.entitlements
ThumbnailExtension/ThumbnailExtension.entitlements

# macOS
.DS_Store

# Local-only icon bake input (ICON.icon stays in git)
ICON.zip

# Python
__pycache__/
*.pyc

# Accidental Peekaboo window captures at worktree root
/window-*.png
