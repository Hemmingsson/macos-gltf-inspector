# Xcode
GLBPreview.xcodeproj/
xcuserdata/
*.xcworkspace
DerivedData/
build/
.build/

# Local research / agent scratch (not the product)
.cursor/
.dex/
plans/
test-models/
Fixtures/
.readme-preview.html
scripts/.cache/

# Generated plists (xcodegen recreates these)
PreviewExtension/Info.plist
ThumbnailExtension/Info.plist
GLBPreview/Info.plist

# Entitlements (xcodegen recreates these)
PreviewExtension/PreviewExtension.entitlements
ThumbnailExtension/ThumbnailExtension.entitlements

# macOS
.DS_Store

# Local-only bake inputs (ICON.icon + DamagedHelmet stay in git)
ICON.zip
screenshots/source_assets/american_tree.glb
